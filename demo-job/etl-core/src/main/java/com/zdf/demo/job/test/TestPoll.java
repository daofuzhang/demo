package com.zdf.demo.job.test;

import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.concurrent.ScheduledExecutorService;

public class TestPoll {

	public static void main(String[] args) {
		/*
		MyTesk thread1=new MyTesk();
		ExecutorService singleThreadPool = Executors.newSingleThreadExecutor();
		singleThreadPool.execute(thread1);
		singleThreadPool.execute(thread1);
		singleThreadPool.execute(thread1);
		*/
		//MyTesk2 thread2=new MyTesk2();
		/*
		ScheduledExecutorService newScheduledThreadPool = Executors.newScheduledThreadPool(10);
		newScheduledThreadPool.execute(thread2);
		newScheduledThreadPool.execute(thread2);
		newScheduledThreadPool.execute(thread2);
		newScheduledThreadPool.shutdown();
		*/
		/*
		ExecutorService newFixedThreadPool = Executors.newFixedThreadPool(2);
		newFixedThreadPool.execute(thread2);
		newFixedThreadPool.execute(thread2);
		newFixedThreadPool.execute(thread2);
		newFixedThreadPool.execute(thread2);
		newFixedThreadPool.execute(thread2);
		*/
		/*
		ExecutorService newCachedThreadPool = Executors.newCachedThreadPool();
		newCachedThreadPool.execute(thread2);
		newCachedThreadPool.execute(thread2);
		newCachedThreadPool.execute(thread2);
		newCachedThreadPool.execute(thread2);
		newCachedThreadPool.execute(thread2);
		*/
	}

}
